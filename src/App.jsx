import Counter from "./components/Counter";

export default function App() {
  return (
    <main className="w-full min-h-screen bg-rich-black text-seasalt font-geist flex items-center justify-center flex-col gap-10">
      <Counter />
    </main>
  );
}
